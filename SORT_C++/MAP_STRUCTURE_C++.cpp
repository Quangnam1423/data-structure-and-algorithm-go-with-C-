#include<iostream>
#include<map>
#include<iterator>
#include<unordered_map>

//Syntax : iterator map_name.insert(iterator position, {key, element})
//Syntax : iterator map_name.insert({key, element})
//Syntax : iterator map_name.insert(iterator position1, iterator position2) similar vector<int> v1(v.begin() , v.end());

int main()
{
	
}
